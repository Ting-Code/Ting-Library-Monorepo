{
  "name": "@config/tsconfig",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "lint:prettier": "prettier --write --cache --ignore-unknown --ignore-path=../../.prettierignore ."
  },
  "files": [
    "base.json",
    "server.json"
  ]
}
